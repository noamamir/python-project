class SubmittedLevel:
    def __init__(self, completionTime, timeLeft, levelPath, levelIndex):
        self.levelIndex = levelIndex
        self.completionTime = completionTime
        self.timeLeft = timeLeft
        self.levelPath = levelPath
